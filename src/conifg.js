/**
 * Conifguration for torrent
 */
export const torrentOptions = {
  announce: [
    'wss://tracker.openwebtorrent.com',
    'wss://tracker.btorrent.xyz',
    'wss://tracker.fastcast.nz'
  ]
}

/**
 * Configuration for renderTo function
 */
export const renderOptions = {
  autoplay: true,
  controls: false
}